# ---- 构建产物 ----
build/
.gradle/
.build-tmp/
captures/
.externalNativeBuild/
.cxx/

# ---- 本机配置（含本机 SDK / JDK 路径，不能提交）----
local.properties
keystore.properties
*.jks
*.keystore

# ---- IDE ----
.idea/
*.iml
*.hprof
.vscode/
.kotlin/

# ---- 杂项 ----
.DS_Store
Thumbs.db
.restore-backup/

# ---- APK ----
# 仓库里只保留 release 包（由 Git LFS 管理，见 .gitattributes）。
# debug 包 52MB，太大，自己用 gradlew assembleDebug 打即可。
APK/*-debug.apk

# 临时文件
.git-commit-msg.txt
.msg.txt

# 真机自测截图（本地留档，不进仓库）
.shots/
